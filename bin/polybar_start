#!/bin/bash

# Quit all running bars
polybar-msg cmd quit

# Wait until the processes have been shut down
while pgrep -u $UID -x polybar >/dev/null; do sleep 2; done

# Launch bar
polybar --config=~/.config/polybar/config.ini -r bar1 & 
